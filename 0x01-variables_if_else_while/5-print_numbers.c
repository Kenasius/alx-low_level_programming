#include <stdio.h>

/**
 * main - This would print numbers from 0 to 9
 *
 * Return: Always return 0 on success
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
