#include "main.h"

/**
 * _isupper - check uppercase letters
 * @c: check char
 * Return: 1 for uppercase and 0 else
 */

int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	else (0);
}
